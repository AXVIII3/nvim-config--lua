\documentclass[12pt]{extarticle} % First number is font size --- Default 11pt, Possible 8pt, 9pt, 10pt, 11pt, 12pt, 14pt, 17pt, 20pt
 
\usepackage{extsizes} % for larger font sizes

% Package for setting custom margins
\usepackage{geometry}

% Packages for formatting tables
\usepackage{longtable} % Allows tables to span multiple pages
\usepackage{booktabs} % Enhances the quality of tables
\usepackage{array} % Provides additional column types for tables

% Package for chemical formulas and equations
\usepackage[version=4]{mhchem}

% Package for hyperlinks and PDF features
\usepackage{hyperref}

% Define custom margins
\geometry{
    left=0.7in,   % Set left margin
    right=0.7in,  % Set right margin
    top=0.7in,    % Set top margin
    bottom=0.7in, % Set bottom margin
}

% Define a new column type for tables
\newcolumntype{P}[1]{>{\raggedright\arraybackslash}p{#1}} % Define a new column type for tables with ragged-right alignment

% Define the tightlist command to adjust spacing in lists
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Begin the document content
\begin{document}

% Insert the body content here
$body$

% End the document
\end{document}
